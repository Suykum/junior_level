package ru.job4j.trackersql;

import org.junit.Test;
import ru.job4j.tracker.Item;

import java.util.ArrayList;

import static org.hamcrest.Matchers.is;
import static org.junit.Assert.*;

public class TrackerSQLTest {
    /*TrackerSQL sql = new TrackerSQL();
    Item item1 = new Item("web", "web page errors");
    Item item2 = new Item("mobile", "mobile app errors");
    Item item3 = new Item("desktop", "desktop software errors");


    @Test
    public void checkConnection() {

        assertThat(sql.init(), is(true));
    }

    @Test
    public void whenCreateTable() {
        sql.createTable();
    }

    @Test
    public void whenInsertItem() {
        sql.add(item1);
        sql.add(item2);
        sql.add(item3);
    }

    @Test
    public void whenReplaceItem() {
        sql.replace("3", new Item("operation system", "Win os not work correctly"));
    }

    @Test
    public void whenDeletingItem() {
        sql.delete("1");
    }

    @Test
    public void whenGettingAllItems() {
        ArrayList<Item> allItems = sql.getAll();
        for (Item i : allItems) {
            System.out.println(i.getId() + " : " + i.getName() + " : " + i.getDesc());
        }
    }

    @Test
    public void whenSearchingByName() {
       ArrayList<Item> byName = sql.findByName("web");
        for (Item i : byName) {
            System.out.println(i.getId() + " : " + i.getName() + " : " + i.getDesc());
        }
    }

    @Test
    public void whenSearchById() {
       Item item = sql.findById("2");
        System.out.println(item.getId() + " : " + item.getName() + " : " + item.getDesc());
    }
*/

}